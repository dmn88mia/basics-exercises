def scream(words)
  words = words + "!!!!"
  return
  puts words
end

scream("Yippeee")

# when the line #3 is reached which contains return the code is done running and does not reach the puts command.